<resources>
    <!-- Compose draws the interface; this theme only needs to supply a window
         with no action bar and a background that matches the first frame. -->
    <style name="Theme.Cobalt" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
